## Tailwind CSS

- Always prioritize use of Tailwind CSS classes instead of a `<style>` tag. Only use `<style>` when an exception is absolutely necessary. Document the exception and justify it to the user.